#ifndef INC_STDAFX_H
#define INC_STDAFX_H

#include <string>

enum VIEWDIMENSION 
{
	TWO_D,
	THREE_D,
};

enum ELEMENTTYPE 
{
	ENTITY,
	RELATIONSHIP,
};

enum SELECTTYPE {
	NONSELECT,
	SELECTENTITY,
	SELECTRELATIONSHIP,
	SELECTREGION,
	SELECT_SCAFFOLD,
};

enum MOUSEACTION{
	NOACTION,
	SELECT,
	MOVEELEMENTS,
	ADDENTITY,
};

enum SELECTSTATE {
	FINISHED,
	SELECTING,
};

enum ELEMENT_COLORSCHEME {
	COLOR_INDEX,
	COLOR_INCIDENCE,
	COLOR_ADJACENCY,
	COLOR_ADJFACTOR,
	COLOR_BETWEENNESS,
	COLOR_ECCENTRICITY,
	COLOR_CLOSENESS,
};

enum ELEMENT_STATISTIC {
	ELEM_INDEX = 0,
	ELEM_INCIDENCE = 1,
	ELEM_ADJACENCY = 2,
	ELEM_REL_ANGLE = 3,
	ELEM_INTERIOR_INCIDENCE = 4,
	ELEM_NONE = 5,
};

enum VISTYPE {	
	PRIMEVIS,
	DUALVIS,
	BIPARTVIS,
};

enum VIEWSTYLE {
	POLYGON,
	COMPOSED,
	BIPARTITE,
};

enum CLUSTER_STATISTIC
{
	CLUSTER_INCIDENCE,
	CLUSTER_ADJACENCY_FACTOR
};

enum OPERATION_TYPE
{
	EDGE_DELETE,
	EDGE_MERGE,
	NODE_DELETE,
	NODE_MERGE
};

enum TERM_CRITERIA
{
	LINEAR,
	NODE_MIN,
	EDGE_MIN
};

enum LABEL_TYPE
{
	LABEL_NONE,
	LABEL_NAME,
	LABEL_INDEX,
	LABEL_OPIDX,
	LABEL_INCIDENCE,
	LABEL_ADJACENCY,
	LABEL_ADJACENCY_FACTOR,
	LABEL_BETWEENNESS,
	LABEL_ECCENTRICITY,
	LABEL_CLOSENESS,
	LABEL_RESISTANCE,
	LABEL_COST,
	LABEL_IMPORTANCE,
	LABEL_PRIORITY
};

enum STARRIZATIONR_FLAG{NOTSTARRIZE, STARRIZE};
enum UPDATESCAFFOLD_FLAG{NOTUPDATESCAFFOLD, UPDATESCAFFOLD};

#define INFITE              1e16
#define EPI                 1e-15
#define LOW_EPI             1e-13
//#define M_PI				3.14159265358979323846   // pi

#endif
