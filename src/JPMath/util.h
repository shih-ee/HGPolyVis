#ifndef INC_JPMATH_UTIL_H
#define INC_JPMATH_UTIL_H

typedef unsigned int uint;

#define foreach(idx, vector) for(uint (idx) = 0; (idx) < (vector).size(); (idx)++)

#define forint forint0

#define forint0(idx, end) for(uint (idx) = 0; (idx) < (end); (idx)++)

#define forint1(idx, end) for(uint (idx) = 1; (idx) <= (end); (idx)++)


























#endif


